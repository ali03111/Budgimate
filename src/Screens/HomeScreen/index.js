import { View, Text } from 'react-native';
import React, { memo } from 'react';

const HomeScreen = () => {
  return (
    <View>
      <Text>index</Text>
    </View>
  );
};

export default memo(HomeScreen);
